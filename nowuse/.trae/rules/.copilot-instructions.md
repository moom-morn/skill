与 Cursor 规则一致：请优先遵循同目录下的 [cursor-core](rules/cursor-core.md)（全局）及 [leisu-admin-project](rules/leisu-admin-project.md)（打开 `*.vue` / `*.js` 时）。

规则文件和技能文件统一存放在 `agents/` 目录，通过 symlink 引用。修改规则只需改 `agents/rules/` 或 `agents/skills/` 下的文件，三处工具自动生效。
