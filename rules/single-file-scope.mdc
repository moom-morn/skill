---
description: 全局单文件操作约束
alwaysApply: true
---

# 单文件操作规则

默认遵循最小读取范围原则，优先只读取完成任务所必需的文件。

当用户消息中提到"当前文件"、"这个文件"、"该文件"，或明确指定单个文件时：

- **只读取用户明确指定的那一个文件**，不主动读取其他文件
- **不操作任何其他文件**，包括相关联的组件、mixins、API 文件等
- 仅根据当前文件自身内容进行分析和优化

**例外**：用户明确说"也读取 xxx"或"需要参考 xxx"时，才允许读取其他文件。
