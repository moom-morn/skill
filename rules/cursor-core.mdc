---
description: 全局回复、扫描缩写、单文件范围、Vue2 兼容与注释要点
alwaysApply: true
---

<!-- 使用：将本文件复制到业务仓库的 .cursor/rules/ -->

# 全局核心规则

## 回复与输出

- 中文简体；结论优先；短句；不复述整题；不提工具名；列表优于长段。
- Plan 只列步骤；Ask 少给未索代码；不主动全库扫、不扩 scope。
- 未要求则不新建 md；优先给出可运行代码，避免只给伪代码。

## 扫描命令

句首写命令（可组合），紧接问题，不另起一行解释命令。

| 短写 | 别名 | 作用 |
|------|------|------|
| `ns` | no-scan 等 | 不搜仓库 |
| `cf` | current-only 等 | 仅当前文件 + @ |
| `ctx` | context-only 等 | 仅对话已有内容 |
| `scan` | explicit-scan 等 | 允许全库搜 |
| `scope:` / `sc:` | — | 后接目录或 glob |

未写命令：最小必要检索。旧词：`无扫描`≈ns，`仅当前文件`≈cf，`仅附件`≈ctx，`显式扫描`≈scan，`范围:`≈scope:。

## 单文件范围

用户提到「当前文件 / 这个文件 / 该文件」或只明确一个文件时：只读、只改该文件，不主动读改关联组件、mixin、API。用户明确说「也读 xxx」「参考 xxx」时可扩展。

## 代码与注释

- Vue2 代码不使用可选链、空值合并（`?.`、`??`）等新语法。
- 关键业务逻辑、数据转换、缓存分支处补短注释，说清「为什么」或「在处理什么」；精炼，写在复杂块上方，不写废话。
