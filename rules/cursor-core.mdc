---
description: Global response style and coding constraints for all interactions. Enforces Chinese replies, minimal scope, Vue 2 compatibility, and comment standards.
alwaysApply: true
---

## 回复规范
- 语言：简体中文
- 结论优先；短句；不复述题目；不提工具名；列表优于长段
- Plan 模式：只列步骤；不主动给未被索要的代码；不扩 scope
- 未被要求时不新建 md 文件；给可运行代码，不给伪代码

## 搜索范围命令（句首写，可组合）

| 命令 | 作用 |
|------|------|
| `ns` | 不搜仓库 |
| `cf` | 仅当前文件 |
| `ctx` | 仅对话内容 |
| `scan` | 允许全库搜 |
| `scope:路径` / `sc:路径` | 限定搜索范围 |

无命令时：最小必要检索。

## 单文件范围约束
用户说「当前/这个/该文件」或只提供一个文件时：只读写该文件，不主动读关联组件/mixin/API。用户明确说「也读 xxx」时可扩展。

## Vue 2 兼容
禁止使用 `?.` `??` 等 ES2020+ 语法，保持 Node 12 旧环境兼容。

## 注释规范
改动代码时，遇到不直观的业务逻辑、数据转换、缓存分支，补短注释说「为什么」或「处理什么」，不写废话注释。
